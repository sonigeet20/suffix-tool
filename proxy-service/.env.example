# Server Configuration
PORT=3000
NODE_ENV=production

# Luna Residential Proxy Credentials
LUNA_PROXY_HOST=customer-USERNAME-sessid-SESSION.proxy.lunaproxy.com
LUNA_PROXY_PORT=12233
LUNA_PROXY_USERNAME=your-username
LUNA_PROXY_PASSWORD=your-password

# API Key for securing your proxy service (optional but recommended)
API_KEY=your-secure-api-key-here

# User Agent Configuration (for high-volume traffic - 500k+ requests/day)
# POOL_SIZE: Number of unique user agents to generate (10000+ recommended for 500k daily calls)
# MODE: 'dynamic' (always fresh, no pool), 'pool' (pre-generated pool), 'hybrid' (pool + dynamic)
USER_AGENT_POOL_SIZE=10000
USER_AGENT_MODE=dynamic
USER_AGENT_REFRESH_INTERVAL_HOURS=12
